
#include <Core/Texture/Texture2D.h>
#include <Core/Texture/Image.h>

namespace Agmd
{
/*	Texture2D::Texture2D(const ivec2& size, TPixelFormat format, bool hasMipmaps, bool autoMipmaps) :
	TextureBase(size,format,hasMipmaps,autoMipmaps),
	m_Data(size,format)
	{}


*/
}
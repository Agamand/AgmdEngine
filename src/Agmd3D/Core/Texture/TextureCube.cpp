
#include <Core/Texture/TextureCube.h>
#include <Core/Texture/Image.h>

namespace Agmd
{
	/*TextureCube::TextureCube(const ivec2& size, TPixelFormat format, bool hasMipmaps, bool autoMipmaps) :
	TextureBase(size,format,hasMipmaps,autoMipmaps)
	{
		for(int32 i = 0; i < MAX_FACE; i++)
			m_Data[i] = Image(size,format);
	}

	TTextureType TextureCube::GetType() const
	{
		return TEXTURE_CUBE;
	}*/
}